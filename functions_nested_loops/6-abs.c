#include "main.h"
#include <stdio.h>

/**
* _abs - computes the absolute value of an integer.
*
* @n : value to compute
*
* Return: Always 0 (Success)
*/
int _abs(int n)

{
	if (n < 0)
{
	return (-n);
}
	else
{
	return (n);
}
}
