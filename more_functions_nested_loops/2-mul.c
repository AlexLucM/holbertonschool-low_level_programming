#include "main.h"
#include <stdio.h>

/**
* mul - multiply two integers
*
* @a : first integer
*
* @b : second integer
*
* Return: Always 0 (Success)
*/
int mul(int a, int b)

{
int result;
{
result = a * b;
	return (result);
}
}
